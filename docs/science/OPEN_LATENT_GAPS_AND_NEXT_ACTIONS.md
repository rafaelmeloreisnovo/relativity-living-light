# Open, Latent, Lateral, Forgotten, and Next Actions Map

Status: gap-to-action map  
Claim level: `claim_allowed=false`

---

## 1. What is open

```text
raw data with checksum
cosmology_background full comparison
claim_allowed automated gate
JSON schemas
unit tests
route-specific baselines
covariance/error models
```

---

## 2. What was left behind

### 2.1 Cosmology core

The cosmology route is still the hardest and most central blocked path. It requires a covariance-aware comparison against LCDM, wCDM, and CPL using BAO, H(z), Pantheon+, Planck/CMB, and f_sigma8.

### 2.2 Residual gravity structures

This route is mapped, but weaker than the others. It still needs raw stream data, a validator, and a stream-membership/residual-mass metric.

### 2.3 CI artifacts vs canonical results

Some artifacts are generated by CI ZIPs. When a CI result becomes the official state, it should be committed under `data/results/` and referenced in docs.

---

## 3. What was obvious but passed by

```text
parser regression tests
claim_allowed=true gate
source license/terms fields
access_date_utc fields
raw_sha256 fields
negative result tracking
route maturity scoring
```

---

## 4. What is latent

```text
exoplanet tidal deformation
galactic geodesy with stellar streams
paper-claim audit engine
scientific due diligence dashboard
SPICE/JPL orbital residual tests
LVK posterior mass-gap tests
```

---

## 5. What is lateral but valuable

### 5.1 Scientific Claim Governance Engine

A general product direction:

```text
hypothesis + data + metric + baseline + uncertainty + artifact
→ claim_allowed true/false
→ why
→ what is missing
→ what would falsify
```

### 5.2 Audit of scientific papers

A lateral application:

```text
claim strength audit
baseline audit
raw data/provenance audit
reproducibility audit
```

### 5.3 Planetary and galactic geodesy

The orbital and residual-structure routes can connect:

```text
planetary geoid / J2 / torque / spin
stellar streams / galactic potential / residual mass
```

---

## 6. Next execution order

```text
P0. raw data manifest and claim_allowed gate
P1. compact_remnant raw posterior ingestion
P1. JSON schemas and tests
P1. orbital SPICE/JPL state-vector ingestion
P2. residual gravity validator
P2. high-z SMBH raw context
P2. historical impulse traceback
P3. exoplanet tidal deformation
P3. paper claim audit productization
```

---

## 7. Safe conclusion

The open space is not empty. It is a ranked map of what must be measured next.
