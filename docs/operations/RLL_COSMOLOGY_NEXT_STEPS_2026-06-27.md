# RLL Cosmology Next Steps — H₀ grid and w_eff→CPL mapping

Status: operational roadmap, claim-blocked.  
Updated: 2026-06-27.

## Why this exists

This note materializes the recommended next-step space after the wandering-BH
ledger work: the immediate cosmology risks are not black-hole metadata, but
background-fit artifacts and whether the RLL effective equation of state can
track a CPL-like DESI DR2 target without overclaiming.

## Route A — H₀ boundary-artifact mitigation

1. Run `scripts/run_h0_grid_expansion.py` on the corrected H₀ interval.
2. Confirm the best H₀ values are internal to the grid, not at min/max.
3. Compare RLL and ΛCDM under the same Hz+BAO inputs.
4. Keep `claim_allowed=false` if the RLL AIC penalty remains high or the H₀
   degeneracy gate fails.

Current generated status: `claim_blocked_quality_gate_failed`.

## Route B — w_eff(z) to CPL target mapping

1. Run `scripts/compute_weff_cpl_mapping.py` to generate the deterministic
   w_eff table and scan summary.
2. Treat the user-provided `(w0, wa)=(-0.838, -0.62)` as a numerical target only
   until exact official table provenance is locally ingested.
3. Keep `TOKEN_VAZIO` for exact DESI best-fit table values until the precise
   data combination, table row, and citation are captured.
4. Reject the restricted RLL mapping when `chi2_weff_vs_cpl >= 10` or when
   present-day `w_eff(z=0)` fails the dark-energy-like gate.

Current generated status: `claim_blocked_falsifier_failed`.

## Enterprise/operational gates

| Gate | Required condition | Rollback |
|---|---|---|
| Failsafe | No output can set `claim_allowed=true` from these scans. | Revert generated JSON/CSV. |
| Failover | If official DESI values are not locally proven, use the user target only as claim-blocked input. | Restore `TOKEN_VAZIO`. |
| Reproducibility | Outputs must be regenerated by one command per route. | Delete outputs and rerun scripts. |
| Mitigation | Boundary optima require grid expansion before interpretation. | Expand grid or lower claim level. |

## Recommended next single action

Ingest exact DESI DR2 CPL table provenance into a small source ledger, then rerun
`compute_weff_cpl_mapping.py` against that verified target. Until then, use the
current results only as a falsifier rehearsal and not as evidence for RLL.
