# RLL BH Flux Note

This note adds a benchmark-only formula layer for accretion flow.

Mdot is the main variable.

Phi_M = Mdot / A_H
Ndot = Mdot / m_unit

RLL_total = RLL_cosmo + RLL_field + RLL_BH_flux

Claim boundary: benchmark only, not proof.
