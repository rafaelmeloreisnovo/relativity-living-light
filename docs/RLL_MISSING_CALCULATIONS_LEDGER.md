# RLL Missing Calculations Ledger — comparação justa RLL × ΛCDM × wCDM × CPL

Escopo da auditoria: varredura local do repositório até 5 níveis de profundidade, sem alterar datasets brutos e sem recalcular resultados cosmológicos. Este ledger separa **cálculo executável**, **rota parcial**, **token vazio** e **referência bibliográfica/documental** para impedir que uma referência externa seja tratada como evidência computacional.

## Critérios de status

- **IMPLEMENTADO** — há função, pipeline ou artefato executável que calcula o item de forma direta para ao menos uma rota de validação.
- **PARCIAL** — há cálculo utilizável, mas limitado por hipótese plana, aproximação, ausência de covariância completa, ausência de dataset materializado, ou ausência de todos os comparadores exigidos.
- **AUSENTE** — não foi localizado nem cálculo nem rota documental operacional relevante.
- **TOKEN_VAZIO** — o item é necessário para a comparação justa, mas falta implementação operacional; deve permanecer explicitamente vazio em vez de ser inferido.
- **EXISTE COMO REFERÊNCIA BIBLIOGRÁFICA, MAS NÃO COMO CÁLCULO** — aparece em texto, motivação ou bibliografia, porém não como cálculo reprodutível no pipeline.

## Ledger

| item | status | arquivo onde existe | fórmula usada | dependência bibliográfica | risco se faltar | próximo cálculo mínimo |
|---|---|---|---|---|---|---|
| 1. H(z) | IMPLEMENTADO | `rll_vs_lcdm.py`; `data/pipelines/structure_d/cosmo.py`; `data/pipelines/structure_d/joint_real_likelihood.py`; `docs/rll_validation_real.py` | `H(z)=H0 E(z)`; LCDM: `sqrt(Ωm(1+z)^3 + 1-Ωm)`; RLL/logístico: `sqrt(E2_RLL)`; extensão RLL soma termos opcionais. | Planck/DESI aparecem como contexto; dados reais vêm de `data/real/Hz_data_real.csv` via config. | Baixo para expansão; alto se o mesmo `H(z)` não for usado de modo idêntico em SN/BAO/CMB. | Centralizar uma API única `H(z)` para LCDM, wCDM, CPL e RLL com testes de regressão. |
| 2. E(z) | IMPLEMENTADO | `rll_vs_lcdm.py`; `data/pipelines/structure_d/cosmo.py`; `data/pipelines/structure_d/joint_real_likelihood.py`; `docs/panteon_likelihood.py` | `E(z)=H(z)/H0`; em vários pontos o código implementa `E²(z)` e depois `sqrt(E²)`. | LCDM/Planck como baseline; RLL usa transição logística autoral/fenomenológica. | Médio: existem múltiplas parametrizações de RLL (`w0,wa`, logística, ΛCDM++), o que pode misturar modelos. | Criar enum/registro de modelos e documentar qual `E(z)` define cada comparação. |
| 3. distância comóvel `D_C` | IMPLEMENTADO | `rll_vs_lcdm.py`; `data/pipelines/structure_d/models.py`; `data/pipelines/structure_d/joint_real_likelihood.py`; `docs/rll_validation_real.py`; `docs/panteon_likelihood.py` | `D_C(z)=c∫_0^z dz'/H(z')`, por Simpson, trapézio ou `scipy.integrate.quad`. | Fórmula cosmológica padrão; dependente do `H(z)` escolhido. | Alto: BAO, CMB shift e SN ficam inconsistentes sem uma integral comum. | Unificar integrador, tolerância e cache; testar `D_C(0)=0` e monotonicidade. |
| 4. distância transversa `D_M` | PARCIAL | `rll_vs_lcdm.py`; `data/pipelines/structure_d/joint_real_likelihood.py` | Em universo plano, usa implicitamente `D_M=D_C`; `DM_over_rd = D_C/r_d`. Não há `S_k(D_C)` para curvatura. | BAO anisotrópico DESI; cosmologia FLRW plana assumida. | Médio/alto: justo apenas se todos os modelos forem comparados sob a mesma hipótese plana; curvatura não pode ser testada. | Implementar `D_M = S_k(D_C)` com `Ω_k`, mantendo default plano. |
| 5. distância luminosa `D_L` | PARCIAL | `docs/panteon_likelihood.py`; fórmula documental em `docs/THEORY_INVENTORY_AND_EXTENSIONS.md` | `D_L=(1+z)D_C` no pipeline Pantheon+; equivale a `D_L=(1+z)D_M` só no caso plano. | Pantheon+ / SNe Ia. | Alto: SN não pode entrar de forma justa sem `D_L` consistente com `D_M`. | Mover `D_L=(1+z)D_M` para módulo compartilhado com curvatura opcional. |
| 6. módulo de distância `mu(z)` | PARCIAL | `docs/panteon_likelihood.py`; referência textual em `docs/THEORY_INVENTORY_AND_EXTENSIONS.md` | `μ=5 log10(D_L*10^6/10) + M_abs` no script Pantheon+. | Pantheon+; calibração de magnitude absoluta/SN. | Alto: sem `μ(z)` com covariância, Pantheon+ não deve sustentar preferência de modelo. | Implementar likelihood SN compartilhada com `M_abs`/nuisance claro e validação contra arquivo Pantheon+. |
| 7. BAO: `D_H/r_d`, `D_M/r_d`, `D_V/r_d` | IMPLEMENTADO | `rll_vs_lcdm.py`; `data/pipelines/structure_d/joint_real_likelihood.py`; `data/pipelines/structure_d/models.py` | `D_H/r_d=(c/H)/r_d`; `D_M/r_d=D_C/r_d` em plano; `D_V/r_d=[z D_M² D_H]^(1/3)/r_d`. | DESI DR2 BAO; legacy BOSS/DESI DV. | Médio: rota antiga usa só `D_V`; comparação moderna precisa anisotrópicos `D_M`/`D_H`. | Preferir sempre `data/real/cosmology/desi_dr2_bao_primary_points.csv` e desativar rota legacy para claims. |
| 8. uso de `r_d` | IMPLEMENTADO | `rll_vs_lcdm.py`; `data/pipelines/structure_d/joint_real_likelihood.py`; `docs/rll_validation_real.py`; `docs/panteon_likelihood.py` | Tanto `r_d` fixo via argumento (`--rs-drag`) quanto aproximação tipo power-law `147.78(Ωm h²/0.1432)^-0.255(Ωb h²/0.02236)^-0.134`. | Eisenstein-Hu/Aubourg/Percival-style; Planck usado como calibração textual. | Alto: fixar `r_d` para um modelo e ajustar em outro seria comparação injusta. | Declarar política única: `r_d` fixo para todos ou derivado para todos, com `Ω_b h²` em `k`. |
| 9. `chi2` diagonal | IMPLEMENTADO | `rll_vs_lcdm.py`; `data/pipelines/structure_d/likelihood.py`; `data/pipelines/structure_d/joint_real_likelihood.py`; `docs/rll_validation_real.py` | `χ²=Σ[(obs-model)/σ]²`. | Qualquer dataset com erros independentes. | Baixo como fallback; alto se usado onde há covariância publicada. | Manter como modo explícito `--diagonal`, nunca silencioso quando covariância existe. |
| 10. `chi2` com covariância completa `C^-1` | PARCIAL | `data/pipelines/structure_d/likelihood.py`; `docs/panteon_likelihood.py`; `data/pipelines/structure_d/joint_real_likelihood.py` | `χ² = rᵀ C⁻¹ r`; no código geral usa solve linear; Pantheon+ inverte `C`; DESI joint monta matriz por blocos DM/DH. | Pantheon+ STAT+SYS; DESI DR2 covariance summary. | Muito alto: sem `C^-1`, AIC/BIC pode favorecer modelo por erro estatístico subestimado. | Usar matrizes completas oficiais por dataset; documentar quando só há bloco/resumo e bloquear claim forte. |
| 11. Pantheon+ | PARCIAL | `docs/panteon_likelihood.py`; `data/pantheon/README.md`; `src/rll/cli.py` | Loader de `lcparam_full_long_zhel.txt` + `Pantheon+SH0ES_STAT+SYS.cov`, `μ(z)`, `χ²=rᵀC⁻¹r`, AIC/BIC. Os arquivos brutos não estão materializados no repositório. | Pantheon+SH0ES DataRelease. | Muito alto: sem os arquivos reais e hashes, SN vira intenção de pipeline, não resultado. | Materializar/validar os dois arquivos externos com SHA256; rodar `rll preflight-real` antes de qualquer claim. |
| 12. DESI DR2 | IMPLEMENTADO | `data/real/cosmology/desi_dr2_bao_primary_points.csv`; `data/real/cosmology/desi_dr2_bao_covariance_summary.csv`; `rll_vs_lcdm.py`; `data/pipelines/structure_d/joint_real_likelihood.py` | Predições BAO `D_M/r_d`, `D_H/r_d`, `D_V/r_d`; covariância por blocos DM/DH e diagonal onde não pareado. | DESI DR2 BAO. | Médio: é utilizável, mas uma claim forte ainda depende da covariância oficial completa e do conjunto conjunto com SN/CMB/growth. | Trocar resumo por matriz completa oficial quando disponível e registrar hashes/proveniência. |
| 13. cronômetros cósmicos H(z) | IMPLEMENTADO | `data/real/Hz_data_real.csv`; `data/pipelines/structure_d/datasets_config.json`; `rll_vs_lcdm.py`; `data/pipelines/structure_d/joint_real_likelihood.py` | `χ²_Hz` compara `H_obs` contra `H0 E(z)` com `σ_H`. | Moresco/BOSS conforme metadados locais. | Médio: CC isolado não resolve degenerescências de distância e crescimento. | Separar CC puros de pontos BAO/Lyα em metadados e permitir covariância se publicada. |
| 14. CMB prior / Planck compressed likelihood | PARCIAL | `data/real/CMB_shift_real.json`; `data/pipelines/structure_d/datasets_config.json`; `data/pipelines/structure_d/joint_real_likelihood.py`; `docs/rll_validation_real.py` | Shift parameters `R=sqrt(Ωm) H0 D_C(z*)/c` e `l_A=πD_C(z*)/r_d`, com erros diagonais. | Planck 2018 VI. | Alto: compressed likelihood sem matriz de covariância Planck pode super/subponderar CMB. | Implementar vetor comprimido com covariância Planck completa ou remover de claims fortes. |
| 15. `w_eff(z)` do RLL | EXISTE COMO REFERÊNCIA BIBLIOGRÁFICA, MAS NÃO COMO CÁLCULO | `docs/README_CIENTIFICO.md`; `docs/LAGRANGIANO_EFT.md`; `docs/COMPARACAO_DESI_2025.md` | Textos citam `w_eff(z)=-f(z)` e transição logística, mas não há função de likelihood/export padronizada `w_eff_RLL(z)`. | Interpretação RLL/DESI e EFT. | Alto: sem `w_eff(z)` computável, não há comparação quantitativa com wCDM/CPL. | Criar função `w_eff_rll(z, params)`, exportar grade e testar limites `[-1,0]`/parâmetros. |
| 16. comparação com wCDM | TOKEN_VAZIO | Não localizada como baseline executável completo. | wCDM mínimo exigiria `E²=Ωm(1+z)^3+Ωr(1+z)^4+ΩDE(1+z)^{3(1+w)}` e ajuste com `k` próprio. | Literatura wCDM padrão. | Muito alto: comparar RLL só contra ΛCDM pode atribuir a RLL uma melhoria que qualquer `w≠-1` produziria. | Implementar modelo wCDM livre, rodar no mesmo vetor de dados/covariância e incluir AIC/AICc/BIC. |
| 17. comparação com CPL / w0waCDM | PARCIAL | `rll_vs_lcdm.py`; `docs/COMPARACAO_DESI_2025.md`; `data2/readme.md` | `rll_vs_lcdm.py` contém forma CPL-like `DE(z)=(1+z)^{3(1+w0+wa)} exp[-3wa z/(1+z)]`, mas ela é usada como “RLL effective extension”, não como baseline CPL independente. | DESI usa parametrização CPL; docs reconhecem necessidade de mapeamento explícito. | Muito alto: sem CPL livre, não se sabe se RLL supera uma dark energy dinâmica padrão. | Criar baseline `CPL` separado, ajustar `w0,wa`, publicar deltas `RLL-CPL` e `CPL-ΛCDM`. |
| 18. AIC | IMPLEMENTADO | `rll_vs_lcdm.py`; `data/pipelines/structure_d/likelihood.py`; `data/pipelines/structure_d/joint_real_likelihood.py`; `docs/panteon_likelihood.py`; `docs/rll_validation_real.py` | `AIC=χ²+2k`. | Critério estatístico padrão. | Alto se `k` incorreto ou datasets/covariância mudam entre modelos. | Garantir que cada modelo reporte `k`, `N`, parâmetros livres e nuisance. |
| 19. AICc | TOKEN_VAZIO | Não localizado como cálculo executável. | `AICc=AIC + 2k(k+1)/(N-k-1)`. | Critério estatístico padrão para amostra finita. | Médio/alto: com `N` pequeno ou `k` alto, RLL pode ser subpenalizado sem AICc. | Implementar `aicc(chi2,k,N)` e bloquear quando `N<=k+1`. |
| 20. BIC | IMPLEMENTADO | `rll_vs_lcdm.py`; `data/pipelines/structure_d/likelihood.py`; `data/pipelines/structure_d/joint_real_likelihood.py`; `docs/panteon_likelihood.py`; `docs/rll_validation_real.py` | `BIC=χ²+k ln N`. | Critério estatístico padrão. | Alto se `N` incluir pontos correlacionados como independentes ou `k` for incoerente. | Padronizar contagem de `N_eff`/`N` e justificar covariâncias. |
| 21. número de parâmetros livres `k` | PARCIAL | `rll_vs_lcdm.py`; `data/pipelines/structure_d/models.py`; `data/pipelines/structure_d/joint_real_likelihood.py`; `docs/panteon_likelihood.py`; `docs/rll_validation_real.py` | Existem valores distintos: ex. `k_lcdm=3/4/5`, `k_rll=5/7/8` dependendo do script e datasets. | Penalização AIC/BIC. | Muito alto: `k` inconsistente muda a conclusão. | Criar tabela única de `k` por modelo/dataset/nuisance e importar em todos os scripts. |
| 22. H0 tension | PARCIAL | `docs/rll_validation_real.py`; valores de `H0` em outputs/leituras de scripts. | Script calcula distância em sigma contra Planck usando `σ_comb=1.73` e imprime comparação com SH0ES, mas não há likelihood SH0ES nem tratamento estatístico completo. | Planck 2018; SH0ES 2022 citado no script. | Alto: dizer “alivia tensão H0” sem likelihood conjunta é claim excessiva. | Implementar diagnóstico formal com priors Planck/SH0ES, covariância e métrica `ΔH0/σ`. |
| 23. S8 tension | TOKEN_VAZIO | Referências textuais a lensing/S8 existem, mas não foi localizado cálculo de tensão `S8`. | Mínimo: `S8=σ8 sqrt(Ωm/0.3)` e comparação com prior/lensing externo. | Weak lensing/KiDS/DES/Planck conforme fonte escolhida. | Alto: crescimento/fσ8 não equivale automaticamente a tensão S8. | Implementar `S8` para cada ajuste e comparar com priors externos versionados. |
| 24. crescimento linear `D+(z)` | TOKEN_VAZIO | Não localizado como solução de crescimento linear. | Exigiria resolver a EDO de crescimento ou integral equivalente para `D_+(z)` normalizado. | Teoria de perturbações lineares. | Alto: sem `D+`, `fσ8` fica proxy fenomenológico. | Implementar solver de `D+` com normalização `D+(0)=1` para cada `E(z)`. |
| 25. `fσ8(z)` | PARCIAL | `data/pipelines/structure_d/growth.py`; `data/pipelines/structure_d/models.py`; `data/pipelines/structure_d/joint_real_likelihood.py`; `data/real/cosmology/fsigma8_growth_real.csv` | Proxy `f≈Ωm(z)^γ`; `fσ8≈σ8 Ωm(z)^0.55`; RLL pode aplicar supressão fenomenológica. | RSD/fσ8 compilations. | Alto: proxy não substitui crescimento linear autoconsistente para RLL/wCDM/CPL. | Substituir proxy por `D+`, `f=dlnD/dlna` e `σ8(z)=σ8,0 D+(z)`. |
| 26. separação entre dado real, sintético e forecast | PARCIAL | `data/pipelines/structure_d/synthetic_real_boundary.py`; `data/pipelines/structure_d/datasets_config.json`; `results/audit/real_data_materialization_audit.md`; `results/synthetic/README.md` | Classificação por `dataset_type`, paths e tokens sintético/mock/fixture; há perfis reais e sintéticos. Forecast não aparece como classe operacional equivalente. | Governança/reprodutibilidade FAIR. | Alto: misturar fixture sintética com dado real gera claim inválida. | Adicionar classe `forecast` explícita e exigir `claim_allowed=false` para sintético/forecast. |
| 27. tabela de origem dos parâmetros: Rafael/RLL versus literatura externa | TOKEN_VAZIO | Há metadados dispersos em configs, docs e outputs, mas não uma tabela canônica parâmetro-origem. | Mínimo: colunas `parâmetro`, `modelo`, `origem`, `fixo/livre/nuisance`, `prior`, `referência`. | Planck/DESI/Pantheon+/RLL autoral. | Muito alto: mistura parâmetro autoral com valor de literatura e contamina AIC/BIC/prior. | Criar `docs/RLL_PARAMETER_ORIGIN_TABLE.md` e um JSON importável pelos pipelines. |

## Diagnóstico conservador da divergência atual

A divergência atual parece vir principalmente de:

- **B) falta de cálculo matemático intermediário** — especialmente `w_eff(z)` operacional, `D_M` com curvatura opcional, `AICc`, `D+(z)` e `S8`.
- **C) falta de dado real/covariância** — Pantheon+ está como pipeline/preflight, mas os arquivos brutos obrigatórios não estão materializados; DESI e CMB usam covariância parcial/compacta.
- **D) excesso de parâmetros livres** — RLL usa mais parâmetros em várias rotas, e o `k` muda entre scripts; isso precisa de tabela única antes de claims.
- **E) comparação injusta contra ΛCDM** — falta baseline wCDM e CPL/w0waCDM livre no mesmo conjunto de dados/covariância.
- **F) mistura entre referência bibliográfica e parâmetro autoral** — há referências a DESI/Planck/Pantheon e fórmulas autorais RLL próximas no texto, mas nem sempre uma tabela separa literatura externa, parâmetro fixado, parâmetro livre e hipótese RLL.

Não há evidência suficiente neste ledger para concluir **A) erro estrutural do RLL**. O estado atual aponta antes para lacunas de cálculo, dados/covariância e desenho de comparação; portanto, também não há base para declarar que RLL vence ΛCDM sem AIC/BIC/AICc consistentes, covariância completa aplicável e baselines wCDM/CPL.
