name: RLL Real Data Orchestrator
on:
  workflow_dispatch:
    inputs:
      pipeline_scope: {type: choice, required: true, default: all, options: [rll, iml, formulas, book, all]}
      dataset_group: {type: choice, required: true, default: all, options: [geomagnetic, heliophysics, cosmology, all]}
      mode: {type: choice, required: true, default: metadata_only, options: [metadata_only, fetch, compute, plots, full]}
      real_data_source: {type: choice, required: true, default: repo, options: [auto, materialized, repo]}
      book_scope: {type: choice, required: true, default: all, options: [methodology, real_data, scripts, observational_validation, desi_boss, amas_mcrp, all]}
      retention_days: {type: choice, required: true, default: '14', options: ['7','14','30','90']}
      fetch_igrf14: {type: boolean, required: true, default: false}
      fetch_wmm2025: {type: boolean, required: true, default: false}
      fetch_omni: {type: boolean, required: true, default: false}
      fetch_nmdb: {type: boolean, required: true, default: false}
      fetch_desi: {type: boolean, required: true, default: false}
      fetch_pantheon: {type: boolean, required: true, default: false}
      fetch_planck: {type: boolean, required: true, default: false}
      fetch_spenvis_reference: {type: boolean, required: true, default: false}
      run_iml: {type: boolean, required: true, default: true}
      run_formulas: {type: boolean, required: true, default: true}
      run_plots: {type: boolean, required: true, default: false}
permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  orchestrate:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: actions/setup-python@v5
        with: {python-version: '3.11'}
      - run: |
          python -m pip install --upgrade pip
          pip install numpy pandas scipy matplotlib requests astropy pyyaml
      - run: mkdir -p artifacts/rll-real-run/{raw,processed,plots,tables,book,iml,formulas,diagnostics,reports}
      - name: Refresh and validate repository inventory
        run: |
          set -euo pipefail
          python3 tools/docs_inventory.py | tee artifacts/rll-real-run/docs_inventory_refresh.log
          python3 tools/docs_inventory.py --check | tee artifacts/rll-real-run/docs_inventory_check.log
      - name: Fetch declared sources only when explicitly enabled
        run: |
          python3 scripts/fetch_real_sources.py \
            --dataset-group "${{ inputs.dataset_group }}" \
            --mode "${{ inputs.mode }}" \
            --output-dir artifacts/rll-real-run \
            ${{ inputs.fetch_igrf14 && '--fetch-igrf14' || '--no-fetch-igrf14' }} \
            ${{ inputs.fetch_wmm2025 && '--fetch-wmm2025' || '--no-fetch-wmm2025' }} \
            ${{ inputs.fetch_omni && '--fetch-omni' || '--no-fetch-omni' }} \
            ${{ inputs.fetch_nmdb && '--fetch-nmdb' || '--no-fetch-nmdb' }} \
            ${{ inputs.fetch_desi && '--fetch-desi' || '--no-fetch-desi' }} \
            ${{ inputs.fetch_pantheon && '--fetch-pantheon' || '--no-fetch-pantheon' }} \
            ${{ inputs.fetch_planck && '--fetch-planck' || '--no-fetch-planck' }} \
            ${{ inputs.fetch_spenvis_reference && '--fetch-spenvis-reference' || '--no-fetch-spenvis-reference' }}
      - if: ${{ inputs.mode == 'compute' || inputs.mode == 'full' }}
        run: python3 scripts/compute_rll_real_pipeline.py --output-dir artifacts/rll-real-run --data-source "${{ inputs.real_data_source }}"
      - name: Run gap scan and RLL diagnostic artifacts
        if: ${{ inputs.mode == 'compute' || inputs.mode == 'full' }}
        run: |
          set -euo pipefail
          mkdir -p artifacts/rll-real-run/diagnostics artifacts/rll-real-run/tables artifacts/rll-real-run/reports

          python3 tools/scan_missing_placeholders.py \
            --out-json artifacts/rll-real-run/diagnostics/missing_placeholder_stub_scan.json \
            --out-csv artifacts/rll-real-run/tables/missing_placeholder_stub_scan.csv \
            --out-md artifacts/rll-real-run/reports/MISSING_PLACEHOLDER_STUB_SCAN.md \
            | tee artifacts/rll-real-run/reports/missing_placeholder_stub_scan.log
          echo "[OK] Missing placeholder/stub scan generated." | tee artifacts/rll-real-run/reports/missing_placeholder_stub_scan.status

          if python3 tools/validate_to_add_migration.py > artifacts/rll-real-run/reports/to_add_migration_validation.log 2>&1; then
            cp data/results/to_add_migration_validation.json artifacts/rll-real-run/diagnostics/to_add_migration_validation.json
            cp docs/TO_ADD_MIGRATION_STATUS.md artifacts/rll-real-run/reports/TO_ADD_MIGRATION_STATUS.md
            echo "[OK] to_Add migration validates." | tee artifacts/rll-real-run/reports/to_add_migration_validation.status
          else
            cat artifacts/rll-real-run/reports/to_add_migration_validation.log
            echo "[BLOQUEADO] to_Add migration validation failed; claim remains blocked." | tee artifacts/rll-real-run/reports/to_add_migration_validation.status
          fi

          if python3 scripts/run_h0_grid_expansion.py \
            --out-json artifacts/rll-real-run/diagnostics/h0_grid_expansion_summary.json \
            --out-csv artifacts/rll-real-run/tables/h0_grid_expansion_scan.csv \
            > artifacts/rll-real-run/reports/h0_grid_expansion.log 2>&1; then
            echo "[OK] H0 grid expansion generated." | tee artifacts/rll-real-run/reports/h0_grid_expansion.status
          else
            cat artifacts/rll-real-run/reports/h0_grid_expansion.log
            echo "[BLOQUEADO] H0 grid expansion failed; claim remains blocked." | tee artifacts/rll-real-run/reports/h0_grid_expansion.status
          fi

          if python3 scripts/map_rll_to_w0wa_eff.py \
            --out-json artifacts/rll-real-run/diagnostics/rll_w0wa_eff_map.json \
            --out-csv artifacts/rll-real-run/tables/rll_w0wa_eff_map.csv \
            > artifacts/rll-real-run/reports/rll_w0wa_eff_map.log 2>&1; then
            echo "[OK] RLL to w0wa mapper generated." | tee artifacts/rll-real-run/reports/rll_w0wa_eff_map.status
          else
            cat artifacts/rll-real-run/reports/rll_w0wa_eff_map.log
            echo "[BLOQUEADO] RLL to w0wa mapper failed; claim remains blocked." | tee artifacts/rll-real-run/reports/rll_w0wa_eff_map.status
          fi
      - if: ${{ inputs.run_plots && (inputs.mode == 'plots' || inputs.mode == 'full' || inputs.mode == 'compute') }}
        run: python3 scripts/generate_rll_plots.py
      - if: ${{ inputs.pipeline_scope == 'book' || inputs.pipeline_scope == 'all' || inputs.pipeline_scope == 'rll' }}
        run: python3 scripts/rll_pipeline.py --book-scope "${{ inputs.book_scope }}" --dataset-group "${{ inputs.dataset_group }}" --mode "${{ inputs.mode == 'full' && 'compute' || inputs.mode }}" --output-dir artifacts/rll-real-run
      - if: ${{ inputs.run_iml && (inputs.pipeline_scope == 'iml' || inputs.pipeline_scope == 'all') }}
        run: |
          printf '{"dados":[1,2,3,4],"coerencia_in":0.7,"coerencia0":0.5,"entropia0":0.5,"estado":"ACTIVE"}\n' > artifacts/rll-real-run/iml/input.json
          python3 tools/iml/iml_pipeline.py --input artifacts/rll-real-run/iml/input.json --output artifacts/rll-real-run/iml/output.json
      - if: ${{ inputs.run_formulas && (inputs.pipeline_scope == 'formulas' || inputs.pipeline_scope == 'all') }}
        run: python3 tools/formula_artifact_builder.py --outdir artifacts/rll-real-run/formulas
      - name: Finalize artifact metadata and validation status
        run: |
          set -euo pipefail
          date -u +"%Y-%m-%dT%H:%M:%SZ" > artifacts/rll-real-run/RUN_UTC.txt
          git rev-parse HEAD > artifacts/rll-real-run/COMMIT_SHA.txt
          cat > artifacts/rll-real-run/COMMIT_POLICY.txt <<'EOF'
          This orchestrator is artifact-only. Commit artifacts through a reviewed PR, not an automatic push.
          Artifact upload is an operational delivery mechanism and does not imply scientific validation, claim acceptance, or evidence that required real-data reports were produced.
          Scientific validation requires VALIDATION_STATUS.json with validated=true and an empty missing_required_artifacts list.
          EOF

          python3 - <<'PY'
          import json
          import os
          from pathlib import Path

          mode = os.environ["RLL_MODE"]
          root = Path("artifacts/rll-real-run")
          required = [
              "MANIFEST.json",
              "COMPUTE_REPORT.md",
              "CLAIM_REFERENCE_AUDIT.md",
          ]
          missing = [name for name in required if not (root / name).is_file()]

          if mode == "metadata_only":
              placeholders = {
                  "DIAGNOSTIC_PLACEHOLDER_MANIFEST.json": json.dumps(
                      {
                          "diagnostic_placeholder": True,
                          "replaces": "MANIFEST.json",
                          "reason": "metadata_only mode does not run the real-data compute pipeline",
                      },
                      indent=2,
                      sort_keys=True,
                  )
                  + "\n",
                  "DIAGNOSTIC_PLACEHOLDER_COMPUTE_REPORT.md": (
                      "# Diagnostic placeholder compute report\n\n"
                      "metadata_only mode did not run the real-data compute pipeline.\n"
                  ),
                  "DIAGNOSTIC_PLACEHOLDER_CLAIM_REFERENCE_AUDIT.md": (
                      "# Diagnostic placeholder claim reference audit\n\n"
                      "metadata_only mode did not run the real-data claim audit pipeline.\n"
                  ),
              }
              for filename, content in placeholders.items():
                  (root / filename).write_text(content, encoding="utf-8")

          status = {
              "validated": not missing,
              "mode": mode,
              "required_artifacts": required,
              "missing_required_artifacts": missing,
          }
          (root / "VALIDATION_STATUS.json").write_text(
              json.dumps(status, indent=2, sort_keys=True) + "\n",
              encoding="utf-8",
          )

          if mode in {"compute", "full"} and missing:
              print(
                  "Missing required real pipeline artifacts for "
                  f"{mode} mode: {', '.join(missing)}"
              )
              raise SystemExit(1)
          PY
          find artifacts/rll-real-run -type f ! -path '*/CHECKSUMS.sha256' -print0 | sort -z | xargs -0 sha256sum > artifacts/rll-real-run/CHECKSUMS.sha256
        env:
          RLL_MODE: ${{ inputs.mode }}
      - uses: actions/upload-artifact@v4
        with:
          name: rll-real-run-${{ github.run_id }}
          path: artifacts/rll-real-run/
          retention-days: ${{ fromJSON(inputs.retention_days) }}
