name: "Análise Bayesiana RLL vs ΛCDM — entrada manual canônica"

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: rll-bayes-manual-${{ github.ref }}
  cancel-in-progress: false

jobs:
  canonical-science:
    name: "Delegar ao gate determinístico FASE 24.1"
    uses: ./.github/workflows/rll-pipeline-linear-completo.yml
    with:
      modo: apenas_ciencia
