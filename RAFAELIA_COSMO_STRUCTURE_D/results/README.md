# Legado (não canônico)

Este diretório não é mais usado para saída oficial.

As saídas do fluxo Structure D são gravadas em:
- `results/structure_d/`

Execução oficial:
`python -m data.pipelines.structure_d.run_all`
